package org.example.finance_tracker.dto;

public record RegisterUserDto(String email,
                              String username,
                              String password
                              ) {
}
